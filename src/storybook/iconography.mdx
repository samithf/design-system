import { Meta, Title, IconGallery, IconItem } from "@storybook/blocks";
import * as Icons from "@storybook/icons";

<Meta title="Tokens/Icons" />

<Title>Icons</Title>
These are the icons being used in our design system. They are available as React
components.

<IconGallery>
    {/* <IconItem name="ArrowDownIcon">
        <Icons.ArrowDownIcon />
    </IconItem> */}
  
  {
    Object.entries(Icons).filter(([name]) => name !== 'iconList').map(([name, IconComponent]) => (
      <IconItem key={name} name={name}>
        <IconComponent />
      </IconItem>
    ))
  }

</IconGallery>
